# قاعدة البيانات
MONGODB_URI=mongodb://localhost:27017/shababna-db
# أو استخدم MongoDB Atlas
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/shababna-db

# مفتاح التشفير للـ JWT
JWT_SECRET=your-super-secret-jwt-key-here-make-it-complex

# بيئة التطوير
NODE_ENV=development

# رقم المنفذ للخادم
PORT=5000

# مجلد رفع الملفات
UPLOAD_DIR=uploads

# الحد الأقصى لحجم الملف (بالبايت)
MAX_FILE_SIZE=5242880

# مفتاح CloudFront أو CDN (اختياري)
CLOUDFRONT_URL=

# إعدادات البريد الإلكتروني (اختياري)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=

# Google Analytics (اختياري)
GA_TRACKING_ID=

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key